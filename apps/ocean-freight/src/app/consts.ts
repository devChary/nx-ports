interface AppMetaProps {
  themeColor: string;
  freightLabel: string;
  freightMode: string;
}

export const APP_META: AppMetaProps = {
  themeColor: '#135DFF',
  freightLabel: 'Ocean',
  freightMode: 'ocean',
};
