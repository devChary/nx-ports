interface AppMetaProps {
  themeColor: string;
  freightLabel: string;
  freightMode: string;
}

export const APP_META: AppMetaProps = {
  themeColor: '#771DFF',
  freightLabel: 'Air',
  freightMode: 'air',
};
